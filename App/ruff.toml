# Support Python 3.10+.
target-version = "py310"
# Set the maximum line length to 100 (per CLAUDE.md coding standards).
line-length = 100

[lint]
# Add the `line-too-long` rule to the enforced rule set.
extend-select = ["E501"]
